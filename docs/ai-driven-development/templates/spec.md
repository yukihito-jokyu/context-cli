# <仕様書タイトル>

仕様書ID: spec-XXX-<slug>

## ゴール

<何を達成したいか>。

## 課題

<誰が、どのような状況で、何に困っているか>。

## 対象ユーザー

<対象ユーザー>。

## ユーザー価値

<問題が解決されたときにユーザーへ返る価値>。

## 成功指標

- 指標: <KPIまたは成功指標>
  - 評価方法: <評価方法>
  - 観測時期: <観測可能になる時期>

## スコープ

- <対象範囲>

## スコープ外

- <今回扱わない範囲>

## ユーザーストーリー

- <ストーリー ID>: <ユーザーストーリー>

## 完成条件

- <ストーリー IDまたは要件名>: <外部から検証可能な具体的な完成条件（受入基準）>

## インターフェース・入出力仕様

### CLIコマンド / 引数 / フラグ

- `context <subcommand> [args] [--flags]`
- 引数の詳細:
  - `<arg_name>`: 説明 (型, 必須/任意)
- フラグの詳細:
  - `--flag_name` (`-f`): 説明 (型, デフォルト値, 必須/任意)

### 入力 (標準入力 / ファイル / 対話UI)

- **標準入力 (stdin)**: <標準入力から流し込むデータ形式や利用有無>
- **設定ファイル**: <読み込む設定ファイルやそのフォーマット、期待される値など>
- **対話UI (TTY)**: <huh等を用いた対話プロンプトの流れや入力値の選択肢>

### 出力 (標準出力 / 標準エラー出力 / 終了コード / ファイル変更)

- **標準出力 (stdout)**: <正常終了時の出力フォーマット、TTY/非TTYによる表示の切り替え、JSON出力の有無など>
- **標準エラー出力 (stderr)**: <異常終了時のエラーフォーマット、スタックトレース出力有無など>
- **終了コード (Exit Code)**:
  - `0`: 正常終了
  - `1`: 一般的なエラー (バリデーションエラーなど)
  - `2`: システム/環境エラー (I/Oエラー、設定不足など)
- **ファイル変更 / ディレクトリ操作**: <コマンド実行によって新規作成・修正されるファイルパスとその変更内容>

## 制約事項

- <技術スタック、環境、互換性に関する制約>

## 非機能要件

- <信頼性、パフォーマンス、セキュリティに関する要件>

## リスク

- <本実装が及ぼすリスク影響の1文メモ（データ破壊、既存機能への影響など）>

## 前提条件

- <AIまたは人間が置いた前提>

## 未解決事項

- <人間判断が必要な未確定事項>

## 技術設計ドラフト（全体概要）

### 処理フローチャート（概要）

<!-- Mermaidでシステム全体の主要な処理の流れ（全体像）を記述。詳細な分岐は各タスク側に記載 -->

### シーケンス図（概要）

<!-- Mermaidで主要なコンポーネント・モジュール間の相互作用（全体像）を記述。局所的なシーケンスは各タスク側に記載 -->

### ファイル配置方針

<!-- 新規作成・変更が想定される主なパッケージや大枠のファイル配置方針とそれぞれの責務を記述 -->

- `[NEW/MODIFY]` [ファイル名](相対パス) : このファイルの役割と実装する処理の概要
